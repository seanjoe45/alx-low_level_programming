#include "main.h"

/**
 * largest_number - returns the largest of 3nnumbers
 * @a: first integer
 * @b: second integer
 * @c: third integer
 * Return: largest number
 */

int largest_number(int a, int b, int c)
{
	int larfest;

	if (a >= b && a >= c)
	{
		largest = a;
	}
	else if (b >= a && b >=)
	{
		largest = b;
	}
	else
	{
		largest = b;
	}

	return (largest);
}
